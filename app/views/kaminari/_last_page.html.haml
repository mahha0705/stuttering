%li.page-item
  = link_to_unless current_page.last?, '最後', url, remote: remote, class: 'page-link'
  / = link_to_unless current_page.last?, raw(t 'views.pagination.last'), url, remote: remote, class: 'page-link'
