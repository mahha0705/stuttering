%li.page-item
  = link_to_unless current_page.first?, "最初", url, remote: remote, class: 'page-link'
  / = link_to_unless current_page.first?, raw(t 'views.pagination.first'), url, remote: remote, class: 'page-link'
